# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# 실험 결과
experiments/
logs/
wandb/
tensorboard/
*.pt
*.pth
*.ckpt

# 데이터 (README는 추적)
data/*
!data/README.md
!data/checkpoints/.gitkeep
!data/trajectories/.gitkeep
!data/results/.gitkeep
!data/datasets/.gitkeep
*.h5
*.hdf5
*.npy
*.npz
*.pkl
*.pickle

# 대용량 데이터셋 (루트 레벨)
/droid/
/bridge_v2/
/openx/
/trajectories/
/results/

# 모델 체크포인트  
checkpoints/
*.safetensors
*.bin

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# 환경 설정
.env
.env.local

# 캐시
.cache/
*.cache

# 임시 파일
tmp/
temp/
*.tmp

# 전처리 비교 샘플 이미지 (문서만 추적, 이미지는 제외)
docs/preprocessing/*/samples/

# 외부 참고 문서 PDF (벤더 자료 등 — 로컬 전용)
docs/*.pdf

# 외부 codebase clone (submodule 아닌 host-local clone)
external/eai-vc/

# Claude Code 로컬 설정 (팀 공유 설정 생기면 개별 force add)
.claude/

# 로컬 워크스테이션 전송용 ckpt 번들 (transient, 환경 간 옮길 때만 생성)
*.tar
*.tar.gz

# 사전학습 진행 중 매 epoch 갱신되는 sample image 디렉토리 (요약 PNG만 추적)
paper_artifacts/*_main_train_samples/