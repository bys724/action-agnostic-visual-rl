# Core dependencies for pretraining, action probing, and the aggregation/stats
# scripts. Versions are pinned to the training/evaluation environment (Python 3.11);
# nearby versions should also work.
torch==2.6.0
numpy==1.26.4
scipy==1.17.1
h5py==3.14.0
imageio==2.37.0
pillow==12.1.1
omegaconf==2.3.0

# BC-Transformer finetuning + LIBERO rollout additionally require the LIBERO
# benchmark and its simulator (robosuite / MuJoCo). Install them separately per
# the official LIBERO instructions -- they are not pinned here:
#   https://github.com/Lifelong-Robot-Learning/LIBERO
#
# CALVIN action probing requires the CALVIN dataset loader dependencies; see the
# official CALVIN repository for environment setup.
